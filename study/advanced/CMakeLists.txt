# ── Advanced Cross-Standard Topics ────────────────────────────────────────────
# Language mechanics, memory model, templates, and compilation model.
# These topics span multiple C++ standards and are essential for expert-level
# mastery. Each sub-topic uses the minimum standard needed for the examples.

message(STATUS "[Advanced] Configuring cross-standard study targets")

add_subdirectory(memory_model)
add_subdirectory(value_categories)
add_subdirectory(templates)
add_subdirectory(compilation)
