# ── C++20 › format ────────────────────────────────────────────────────────────
# Requires GCC ≥ 13, Clang ≥ 14 (with libc++), or MSVC 2019 v16.10+.
add_study_target(
    NAME     cpp20_format
    STANDARD 20
    SOURCES  main.cpp
)
