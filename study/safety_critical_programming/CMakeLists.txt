# ── Safety-Critical Real-Time Embedded Programming ───────────────────────────
# Modern C++ techniques for deterministic, testable, resource-bounded automotive
# software. Each subdirectory is one self-contained lesson.

message(STATUS "[Safety-Critical] Configuring study targets")

add_subdirectory(event_and_dynamic_allocation)
